-En el commander (CMD) ejecutar lo siguiente:
	
	python -m pip install --upgrade pip
	pip install pyserial pyodbc pywin32
	pip install pywin32
	pip install psutil

	pip install mysql-connector-python
	pip install tabulate
	pip install pandas openpyxl fpdf pyodbc
	pip install tkcalendar
	pip install pyinstaller



-este se ejecuta en setup comand promp del com0com (previamente instalado)
install PortName=COM6 PortName=COM7


-para instalar MySQL primero instalo el típico, pongo root y clave:bascula2025
no modifico puerto 3306
luego le instalo el programa workbench


